
const age = 40;
const myName = "ken";

const isMyHomeNYC = true;
const isMyHomeSeoul = false;

const myWallet = null;
const myLife = undefined;

// 일반 객체
const object = {
  age: 40, // object.age 에 들어가는 값
};

// 배열
const list = [1, 2, 3];

// 함수
const doSomething = function () {
  console.log(40);
};

doSomething();
console.log(age);
console.log(object.age);
